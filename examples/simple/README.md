# Simple Example

This example demonstrates how to create a basic OpenStack Object Storage account without any specific containers defined, just an account with metadata.

## Usage

To run this example, execute the following commands:

```bash
terraform init
terraform plan
terraform apply
```
