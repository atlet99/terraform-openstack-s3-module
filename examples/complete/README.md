# Complete Example

This example demonstrates how to create an OpenStack Object Storage account and multiple containers with specific configurations, such as versioning, metadata, and access controls.

## Usage

To run this example, execute the following commands:

```bash
terraform init
terraform plan
terraform apply
```
